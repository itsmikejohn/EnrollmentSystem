export type NavItems = {
    name: string,
    url: string,
}

export type ListOfCourses = {
    title: string,
    courses: string[]
}

export type ListOfRequirements = {
    title: string,
    req: string[],
}